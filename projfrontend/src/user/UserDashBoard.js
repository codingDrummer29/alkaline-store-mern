import React from "react";
import Base from "../core/Base";

const UserDashBoard = () => {
  return (
    <div>
      <Base title="User Dashboard">
        <h2 className="text-white">This is User Dashboard page</h2>
      </Base>
    </div>
  );
};

export default UserDashBoard;
